generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}

model Item {
  id        String   @id
  userId    String
  createdAt DateTime @default(now())
  updatedAt DateTime
  value     Int
}
